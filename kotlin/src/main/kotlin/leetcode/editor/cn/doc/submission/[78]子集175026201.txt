class Solution {
    public List<List<Integer>> subsets(int[] nums) {
        List<List<Integer>> ans=new ArrayList();
        List<Integer> path=new ArrayList();
        sub(ans,path,0,nums);
        return ans;
    }

    public void sub(List ans,List<Integer> path,int index,int[] nums){
        if(index==nums.length){
            ans.add(new ArrayList(path));
            return;
        }
        sub(ans,path,index+1,nums);
        path.add(nums[index]);
        sub(ans,path,index+1,nums);
        path.remove(path.size()-1);
    }
}
//runtime:1 ms
//memory:38.7 MB
